# Multi-Team Airflow Worker Service Accounts with Workload Identity

---
# Team 1: Alvi
apiVersion: v1
kind: ServiceAccount
metadata:
  name: airflow-worker-team-alvi
  namespace: ns-cs9-test
  annotations:
    azure.workload.identity/client-id: "PLACEHOLDER_ALVI_MANAGED_IDENTITY_CLIENT_ID"
  labels:
    azure.workload.identity/use: "true"
    team: "alvi"

---
# Team 2: RisikoVVP
apiVersion: v1
kind: ServiceAccount
metadata:
  name: airflow-worker-team-risiko-vvp
  namespace: ns-cs9-test
  annotations:
    azure.workload.identity/client-id: "PLACEHOLDER_RISIKO_VVP_MANAGED_IDENTITY_CLIENT_ID"
  labels:
    azure.workload.identity/use: "true"
    team: "risiko-vvp"

---
# Team 3: TotMort
apiVersion: v1
kind: ServiceAccount
metadata:
  name: airflow-worker-team-tot-mort
  namespace: ns-cs9-test
  annotations:
    azure.workload.identity/client-id: "PLACEHOLDER_TOT_MORT_MANAGED_IDENTITY_CLIENT_ID"
  labels:
    azure.workload.identity/use: "true"
    team: "tot-mort"

---
# Team 4: VakDek
apiVersion: v1
kind: ServiceAccount
metadata:
  name: airflow-worker-team-vak-dek
  namespace: ns-cs9-test
  annotations:
    azure.workload.identity/client-id: "PLACEHOLDER_VAK_DEK_MANAGED_IDENTITY_CLIENT_ID"
  labels:
    azure.workload.identity/use: "true"
    team: "vak-dek"

---
# Team 5: VakSik
apiVersion: v1
kind: ServiceAccount
metadata:
  name: airflow-worker-team-vak-sik
  namespace: ns-cs9-test
  annotations:
    azure.workload.identity/client-id: "PLACEHOLDER_VAK_SIK_MANAGED_IDENTITY_CLIENT_ID"
  labels:
    azure.workload.identity/use: "true"
    team: "vak-sik"

---
# Team 6: VaxEff_MIST
apiVersion: v1
kind: ServiceAccount
metadata:
  name: airflow-worker-team-vax-eff-mist
  namespace: ns-cs9-test
  annotations:
    azure.workload.identity/client-id: "PLACEHOLDER_VAX_EFF_MIST_MANAGED_IDENTITY_CLIENT_ID"
  labels:
    azure.workload.identity/use: "true"
    team: "vax-eff-mist"

---
# Team 7: VaxEff_VVP
apiVersion: v1
kind: ServiceAccount
metadata:
  name: airflow-worker-team-vax-eff-vvp
  namespace: ns-cs9-test
  annotations:
    azure.workload.identity/client-id: "PLACEHOLDER_VAX_EFF_VVP_MANAGED_IDENTITY_CLIENT_ID"
  labels:
    azure.workload.identity/use: "true"
    team: "vax-eff-vvp"
